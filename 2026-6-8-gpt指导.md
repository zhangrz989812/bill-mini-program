# WorkBuddy 长时间开发任务说明：为 bill-mini-program 接入 DeepSeek AI 账单助手

## 0. 任务目标

请直接基于当前 GitHub 仓库进行开发：

```text
https://github.com/zhangrz989812/bill-mini-program
```

目标是在现有微信小程序记账本项目中，完成一个可实际运行的“AI 账单助手”功能。

项目特点：

```text
1. 这是个人使用的小程序，不是面向多人商业化的 SaaS 产品。
2. 项目完全依赖微信小程序原生开发 + 微信云开发 / 腾讯云。
3. 不要引入独立后端，不要引入 Express / Spring Boot / FastAPI。
4. AI 服务使用 DeepSeek API。
5. DeepSeek API Key 不能写在小程序前端代码里，必须放在云函数环境变量中。
6. 第一版不要做复杂 Agent，不要做 AI 自动生成数据库查询，不要做复杂意图识别。
7. 第一版重点是：真实记账入库 → 真实统计 → 云函数调用 DeepSeek → AI 根据账单摘要进行分析。
```

最终目标：

```text
用户在小程序中记账
→ 账单数据保存到微信云数据库 records 集合
→ 统计页可以基于真实 records 数据展示月度统计
→ 用户进入 AI 账单助手页面
→ 输入问题，例如“分析本月消费”
→ 小程序调用 ai-chat 云函数
→ ai-chat 云函数读取本月账单摘要
→ ai-chat 云函数调用 DeepSeek API
→ 小程序展示 AI 分析结果
```

一句话原则：

```text
系统负责算账，DeepSeek 负责解释账。
```

---

## 1. 工作方式要求

请把这次任务当成长时间工程任务处理，不要只改一两个文件就结束。

请按以下要求执行：

```text
1. 先完整扫描仓库结构，确认现有页面、云函数目录、配置文件、依赖文件。
2. 不要假设 README 一定完全等于真实代码，以真实文件为准。
3. 不要删除现有功能。
4. 不要破坏现有页面结构和样式风格。
5. 优先复用现有页面数据结构，减少大改。
6. 每完成一个阶段，要自检相关文件是否能连起来。
7. 遇到缺失目录时自行创建。
8. 遇到云环境 ID、DeepSeek API Key 这种必须由用户配置的信息时，使用清晰占位符和文档说明，不要编造真实值。
9. 如果某些腾讯云 / 微信云开发控制台配置无法通过代码完成，请写入最终交付说明。
10. 不要因为无法实际部署云函数就停止开发，要把代码、目录、说明、配置步骤都补齐。
11. 代码要尽量完整，不要只写伪代码。
12. 最终必须输出开发完成总结、改动文件清单、用户需要手动配置的事项、测试步骤。
```

如果一次运行时间很长，请按阶段持续推进，不要中途停在“建议你下一步做什么”。你需要尽可能直接改代码。

---

## 2. 当前项目判断

请先检查项目中是否已有以下文件：

```text
app.js
app.json
app.wxss
project.config.json
package.json
pages/index/index.js
pages/index/index.wxml
pages/index/index.wxss
pages/record/record.js
pages/record/record.wxml
pages/record/record.wxss
pages/statistics/statistics.js
pages/statistics/statistics.wxml
pages/statistics/statistics.wxss
pages/profile/profile.js
pages/profile/profile.wxml
pages/profile/profile.wxss
cloud/functions
```

根据目前已知情况，项目大概率是：

```text
微信小程序原生开发
微信云开发
微信云数据库
云函数 Node.js
```

并且现有页面中有不少模拟数据。需要重点检查：

```text
1. pages/record/record.js 中 saveRecord 是否只是 console.log 或 setTimeout 模拟保存。
2. pages/index/index.js 中 loadData 是否使用写死的月度收入、支出、最近记录。
3. pages/statistics/statistics.js 中 loadStatistics 是否使用写死的统计数据和图表数据。
4. app.js 中 wx.cloud.init 的 env 是否仍然是 your-env-id。
5. app.json 中是否已经开启 "cloud": true。
```

如果确认存在模拟数据，请按本任务要求替换为真实云数据库逻辑。

---

## 3. 总体实现方案

本次不要做复杂架构。实现方案固定为：

```text
微信小程序页面
  ↓
wx.cloud.callFunction
  ↓
微信云函数
  ↓
微信云数据库 records 集合
  ↓
云函数整理账单摘要
  ↓
云函数调用 DeepSeek API
  ↓
返回 AI 回答给小程序页面
```

新增或改造内容：

```text
1. records 集合数据结构设计。
2. record-add 云函数：新增记账记录。
3. statistics-get 云函数：获取指定月份统计摘要。
4. ai-chat 云函数：获取本月账单摘要并调用 DeepSeek。
5. ai-chat 页面：展示 AI 对话。
6. 首页或我的页增加 AI 账单助手入口。
7. 修改记账页，让它真正写入云数据库。
8. 修改统计页，让它读取真实统计数据。
9. 必要时修改首页，让它读取真实概览数据。
10. 增加配置说明文档。
```

第一版不做：

```text
1. AI 设置页。
2. 多模型切换页面。
3. 多会话管理。
4. 聊天记录入库。
5. AI 自动生成数据库查询。
6. 复杂 Agent 工具调用。
7. 复杂意图识别。
8. 图片识别票据。
9. 语音记账。
10. 预算预测。
```

---

## 4. 必须遵守的安全规则

### 4.1 DeepSeek API Key 不能放前端

禁止在以下位置写入 DeepSeek API Key：

```text
app.js
app.json
任意 pages 目录下的 js 文件
任意 wxml / wxss 文件
前端 utils 文件
project.config.json
README 明文示例
```

API Key 只能来自云函数环境变量：

```text
DEEPSEEK_API_KEY
```

### 4.2 DeepSeek 调用必须走云函数

小程序前端只能调用：

```js
wx.cloud.callFunction({
  name: 'ai-chat',
  data: {
    message: content
  }
})
```

不能直接从小程序发 HTTP 请求到 DeepSeek。

### 4.3 AI 不直接查数据库

不要做：

```text
用户问题 → DeepSeek 生成数据库查询条件 → 云函数执行
```

正确做法：

```text
云函数固定查询 records 集合
→ 云函数计算账单摘要
→ 把摘要给 DeepSeek
→ DeepSeek 负责解释
```

### 4.4 只传摘要，不传全部明细

第一版传给 DeepSeek 的内容应包括：

```text
本月总收入
本月总支出
本月结余
本月账单笔数
分类支出排行
最近 7 天支出
本月大额支出 Top5
```

不要把所有 records 明细完整塞进 DeepSeek。

---

## 5. 云数据库设计

### 5.1 records 集合

请使用集合名：

```text
records
```

每条记录结构：

```json
{
  "_openid": "微信 openid，由云函数自动写入",
  "type": "expense",
  "amount": 45.5,
  "categoryId": 1,
  "categoryName": "餐饮",
  "categoryIcon": "🍜",
  "categoryColor": "#ff9500",
  "date": "2026-06-07",
  "month": "2026-06",
  "remark": "午餐",
  "images": [],
  "createdAt": 1780000000000,
  "updatedAt": 1780000000000
}
```

字段说明：

```text
_openid：云函数根据 cloud.getWXContext().OPENID 自动写入。
type：income 或 expense。
amount：正数，不要存负数。
categoryId：分类 ID。
categoryName：分类名称。
categoryIcon：分类图标。
categoryColor：分类颜色。
date：格式 YYYY-MM-DD。
month：格式 YYYY-MM，从 date 截取，用于月份查询。
remark：备注，可为空字符串。
images：图片 URL 数组，可为空数组。
createdAt：时间戳。
updatedAt：时间戳。
```

注意：

```text
收入和支出都用正数保存，通过 type 区分。
不要用 amount 正负号判断收入支出。
```

---

## 6. 云函数目录要求

如果当前项目没有云函数目录，请创建：

```text
cloud/functions
```

本次至少新增三个云函数：

```text
cloud/functions/record-add
cloud/functions/statistics-get
cloud/functions/ai-chat
```

每个云函数目录下至少包含：

```text
index.js
package.json
```

如果微信开发者工具需要额外配置，请按标准微信云函数目录结构处理。

---

## 7. record-add 云函数

### 7.1 功能

保存用户新增的记账记录。

### 7.2 输入

```json
{
  "type": "expense",
  "amount": 45.5,
  "categoryId": 1,
  "categoryName": "餐饮",
  "categoryIcon": "🍜",
  "categoryColor": "#ff9500",
  "date": "2026-06-07",
  "remark": "午餐",
  "images": []
}
```

### 7.3 输出

成功：

```json
{
  "success": true,
  "id": "云数据库记录ID"
}
```

失败：

```json
{
  "success": false,
  "message": "错误原因"
}
```

### 7.4 校验规则

```text
1. type 必须是 income 或 expense。
2. amount 必须能转为数字，并且大于 0。
3. date 不能为空，且应为 YYYY-MM-DD 格式。
4. categoryName 不能为空。
5. images 如果没有传，则默认 []。
6. remark 如果没有传，则默认 ''。
```

### 7.5 代码要求

请实现完整 `cloud/functions/record-add/index.js`。

参考代码：

```js
const cloud = require('wx-server-sdk')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = cloud.database()

function isValidDateText(date) {
  return typeof date === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(date)
}

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  const openid = wxContext.OPENID

  const {
    type,
    amount,
    categoryId,
    categoryName,
    categoryIcon,
    categoryColor,
    date,
    remark,
    images
  } = event

  if (!['income', 'expense'].includes(type)) {
    return {
      success: false,
      message: '记录类型不正确'
    }
  }

  const realAmount = Number(amount)

  if (!realAmount || realAmount <= 0) {
    return {
      success: false,
      message: '金额不正确'
    }
  }

  if (!isValidDateText(date)) {
    return {
      success: false,
      message: '日期格式不正确'
    }
  }

  if (!categoryName) {
    return {
      success: false,
      message: '分类不能为空'
    }
  }

  const now = Date.now()
  const month = date.slice(0, 7)

  const record = {
    _openid: openid,
    type,
    amount: Number(realAmount.toFixed(2)),
    categoryId,
    categoryName,
    categoryIcon: categoryIcon || '',
    categoryColor: categoryColor || '#8e8e93',
    date,
    month,
    remark: remark || '',
    images: Array.isArray(images) ? images : [],
    createdAt: now,
    updatedAt: now
  }

  try {
    const res = await db.collection('records').add({
      data: record
    })

    return {
      success: true,
      id: res._id
    }
  } catch (error) {
    console.error('record-add error:', error)

    return {
      success: false,
      message: '保存记录失败',
      errorMessage: error.message
    }
  }
}
```

### 7.6 package.json

```json
{
  "name": "record-add",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
```

---

## 8. 修改记账页 saveRecord

请修改：

```text
pages/record/record.js
```

目标：

```text
把当前模拟保存逻辑替换成 wx.cloud.callFunction 调用 record-add。
```

要求：

```text
1. 保留现有金额、分类、日期、备注、图片的页面逻辑。
2. 保留现有保存前校验。
3. 构造 record 对象后，调用 record-add 云函数。
4. 保存成功后提示“保存成功”，然后返回上一页。
5. 保存失败时提示云函数返回的 message。
6. 不要再只 console.log 后 setTimeout 模拟成功。
```

参考实现：

```js
wx.showLoading({
  title: '保存中...'
})

wx.cloud.callFunction({
  name: 'record-add',
  data: record,
  success: (res) => {
    wx.hideLoading()

    const result = res.result || {}

    if (result.success) {
      wx.showToast({
        title: '保存成功',
        icon: 'success'
      })

      setTimeout(() => {
        wx.navigateBack()
      }, 1000)
    } else {
      wx.showToast({
        title: result.message || '保存失败',
        icon: 'none'
      })
    }
  },
  fail: (err) => {
    wx.hideLoading()
    console.error('record-add 调用失败:', err)

    wx.showToast({
      title: '保存失败',
      icon: 'none'
    })
  }
})
```

---

## 9. statistics-get 云函数

### 9.1 功能

根据指定月份，从 `records` 集合统计：

```text
1. 总收入
2. 总支出
3. 结余
4. 账单笔数
5. 分类支出排行
6. 每日收支趋势
7. 大额支出 Top5
8. 最近记录
```

### 9.2 输入

```json
{
  "month": "2026-06"
}
```

如果不传 month，默认当前月份。

### 9.3 输出

```json
{
  "success": true,
  "summary": {
    "month": "2026-06",
    "totalIncome": 8500,
    "totalExpense": 5230.5,
    "balance": 3269.5,
    "recordCount": 36,
    "categoryStats": [],
    "dailyStats": [],
    "topExpenses": [],
    "recentRecords": []
  }
}
```

### 9.4 分页要求

微信云数据库单次查询有限制，不能只写 `.limit(1000)` 就彻底结束。

请封装一个分页查询函数：

```js
async function getAllRecords(openid, month) {
  const pageSize = 100
  let skip = 0
  let all = []

  while (true) {
    const res = await db.collection('records')
      .where({
        _openid: openid,
        month
      })
      .skip(skip)
      .limit(pageSize)
      .get()

    const data = res.data || []
    all = all.concat(data)

    if (data.length < pageSize) {
      break
    }

    skip += pageSize
  }

  return all
}
```

### 9.5 代码要求

请实现完整：

```text
cloud/functions/statistics-get/index.js
```

参考代码：

```js
const cloud = require('wx-server-sdk')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = cloud.database()

function getCurrentMonth() {
  const now = new Date()
  const year = now.getFullYear()
  const month = String(now.getMonth() + 1).padStart(2, '0')
  return `${year}-${month}`
}

function toMoney(value) {
  return Number(Number(value || 0).toFixed(2))
}

async function getAllRecords(openid, month) {
  const pageSize = 100
  let skip = 0
  let all = []

  while (true) {
    const res = await db.collection('records')
      .where({
        _openid: openid,
        month
      })
      .skip(skip)
      .limit(pageSize)
      .get()

    const data = res.data || []
    all = all.concat(data)

    if (data.length < pageSize) {
      break
    }

    skip += pageSize
  }

  return all
}

function buildSummary(records, month) {
  const incomeRecords = records.filter(item => item.type === 'income')
  const expenseRecords = records.filter(item => item.type === 'expense')

  const totalIncome = incomeRecords.reduce((sum, item) => {
    return sum + Number(item.amount || 0)
  }, 0)

  const totalExpense = expenseRecords.reduce((sum, item) => {
    return sum + Number(item.amount || 0)
  }, 0)

  const categoryMap = {}
  const dailyMap = {}

  records.forEach(item => {
    const day = item.date ? item.date.slice(5) : ''
    if (!day) return

    if (!dailyMap[day]) {
      dailyMap[day] = {
        date: day,
        income: 0,
        expense: 0
      }
    }

    const amount = Number(item.amount || 0)

    if (item.type === 'income') {
      dailyMap[day].income += amount
    }

    if (item.type === 'expense') {
      dailyMap[day].expense += amount
    }
  })

  expenseRecords.forEach(item => {
    const amount = Number(item.amount || 0)
    const categoryName = item.categoryName || '其他'

    if (!categoryMap[categoryName]) {
      categoryMap[categoryName] = {
        name: categoryName,
        amount: 0,
        count: 0,
        color: item.categoryColor || '#8e8e93'
      }
    }

    categoryMap[categoryName].amount += amount
    categoryMap[categoryName].count += 1
  })

  const categoryStats = Object.values(categoryMap)
    .map(item => {
      return {
        ...item,
        amount: toMoney(item.amount),
        percentage: totalExpense > 0
          ? Number((item.amount / totalExpense * 100).toFixed(1))
          : 0
      }
    })
    .sort((a, b) => b.amount - a.amount)

  const dailyStats = Object.values(dailyMap)
    .map(item => {
      return {
        date: item.date,
        income: toMoney(item.income),
        expense: toMoney(item.expense)
      }
    })
    .sort((a, b) => a.date.localeCompare(b.date))

  const topExpenses = expenseRecords
    .slice()
    .sort((a, b) => Number(b.amount || 0) - Number(a.amount || 0))
    .slice(0, 5)
    .map(item => {
      return {
        id: item._id,
        date: item.date,
        categoryName: item.categoryName || '其他',
        amount: toMoney(item.amount),
        remark: item.remark || ''
      }
    })

  const recentRecords = records
    .slice()
    .sort((a, b) => {
      const aTime = a.createdAt || 0
      const bTime = b.createdAt || 0
      return bTime - aTime
    })
    .slice(0, 10)
    .map(item => {
      return {
        id: item._id,
        type: item.type,
        categoryName: item.categoryName || '其他',
        categoryIcon: item.categoryIcon || '',
        categoryColor: item.categoryColor || '#8e8e93',
        amount: toMoney(item.amount),
        date: item.date,
        remark: item.remark || ''
      }
    })

  return {
    month,
    totalIncome: toMoney(totalIncome),
    totalExpense: toMoney(totalExpense),
    balance: toMoney(totalIncome - totalExpense),
    recordCount: records.length,
    categoryStats,
    dailyStats,
    topExpenses,
    recentRecords
  }
}

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  const openid = wxContext.OPENID
  const month = event.month || getCurrentMonth()

  try {
    const records = await getAllRecords(openid, month)
    const summary = buildSummary(records, month)

    return {
      success: true,
      summary
    }
  } catch (error) {
    console.error('statistics-get error:', error)

    return {
      success: false,
      message: '获取统计数据失败',
      errorMessage: error.message
    }
  }
}
```

### 9.6 package.json

```json
{
  "name": "statistics-get",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
```

---

## 10. 修改统计页

请修改：

```text
pages/statistics/statistics.js
```

目标：

```text
把模拟统计数据替换成 statistics-get 云函数返回的真实数据。
```

要求：

```text
1. 保留当前 timeRange、currentDate、statistics、chartData、loading 数据结构。
2. 如果当前页面支持 month/year，第一版优先保证 month 可用。
3. currentDate 如果是 YYYY-MM，则直接作为 month 传给云函数。
4. 云函数返回 summary 后，转换为页面已有结构。
5. categoryStats 字段尽量兼容现有 wxml。
6. chartData 使用 summary.dailyStats。
```

参考逻辑：

```js
loadStatistics: function () {
  this.setData({ loading: true })

  const month = this.data.currentDate

  wx.cloud.callFunction({
    name: 'statistics-get',
    data: {
      month
    },
    success: (res) => {
      const result = res.result || {}

      if (!result.success) {
        this.setData({ loading: false })
        wx.showToast({
          title: result.message || '统计加载失败',
          icon: 'none'
        })
        return
      }

      const summary = result.summary || {}

      this.setData({
        statistics: {
          totalIncome: summary.totalIncome || 0,
          totalExpense: summary.totalExpense || 0,
          balance: summary.balance || 0,
          categoryStats: summary.categoryStats || []
        },
        chartData: summary.dailyStats || [],
        loading: false
      })
    },
    fail: (err) => {
      console.error('statistics-get 调用失败:', err)
      this.setData({ loading: false })

      wx.showToast({
        title: '统计加载失败',
        icon: 'none'
      })
    }
  })
}
```

如果 wxml 中使用的是 `category.name`、`category.amount`、`category.percentage`、`category.color`，请保持字段一致。

---

## 11. 修改首页

请修改：

```text
pages/index/index.js
```

目标：

```text
首页月度概览和最近记录不要继续使用模拟数据。
```

方案：

```text
首页可以直接调用 statistics-get 云函数，取当前月份 summary。
```

要求：

```text
1. 保留 monthOverview 数据结构：totalIncome、totalExpense、balance。
2. recentRecords 使用 summary.recentRecords。
3. 如果首页 wxml 使用 category 字段，而 summary 返回 categoryName，请做字段适配。
4. 页面加载失败时不要白屏，给出 Toast。
```

参考逻辑：

```js
getCurrentMonth: function () {
  const today = new Date()
  const year = today.getFullYear()
  const month = String(today.getMonth() + 1).padStart(2, '0')
  return `${year}-${month}`
},

loadData: function () {
  this.setData({ loading: true })

  wx.cloud.callFunction({
    name: 'statistics-get',
    data: {
      month: this.getCurrentMonth()
    },
    success: (res) => {
      const result = res.result || {}

      if (!result.success) {
        this.setData({ loading: false })
        wx.showToast({
          title: result.message || '数据加载失败',
          icon: 'none'
        })
        return
      }

      const summary = result.summary || {}

      const recentRecords = (summary.recentRecords || []).map(item => {
        return {
          id: item.id,
          type: item.type,
          category: item.categoryName,
          amount: item.type === 'expense' ? -item.amount : item.amount,
          date: item.date,
          remark: item.remark
        }
      })

      this.setData({
        monthOverview: {
          totalIncome: summary.totalIncome || 0,
          totalExpense: summary.totalExpense || 0,
          balance: summary.balance || 0
        },
        recentRecords,
        loading: false
      })
    },
    fail: (err) => {
      console.error('首页数据加载失败:', err)

      this.setData({ loading: false })

      wx.showToast({
        title: '数据加载失败',
        icon: 'none'
      })
    }
  })
}
```

请根据现有 `index.wxml` 实际字段做必要适配。

---

## 12. ai-chat 云函数

### 12.1 功能

负责：

```text
1. 接收用户问题。
2. 获取当前用户 openid。
3. 查询本月 records 数据。
4. 计算账单摘要。
5. 组织 Prompt。
6. 调用 DeepSeek Chat Completions API。
7. 返回 AI 分析结果。
```

第一版固定使用本月账单摘要，不做复杂意图识别。

### 12.2 输入

```json
{
  "message": "帮我分析本月消费"
}
```

### 12.3 输出

成功：

```json
{
  "success": true,
  "reply": "AI 分析内容",
  "summary": {
    "month": "2026-06",
    "totalIncome": 8500,
    "totalExpense": 5230.5
  }
}
```

失败：

```json
{
  "success": false,
  "reply": "AI 分析失败，请稍后再试。",
  "message": "错误原因"
}
```

### 12.4 环境变量

请在代码中读取以下环境变量：

```text
DEEPSEEK_BASE_URL
DEEPSEEK_API_KEY
DEEPSEEK_MODEL
```

默认值建议：

```text
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
```

注意：

```text
不要在代码里写真实 API Key。
DEEPSEEK_MODEL 允许用户在腾讯云 / 微信云函数环境变量里自行调整。
如果当前 DeepSeek 官方推荐模型发生变化，用户只需要改环境变量。
```

### 12.5 依赖

`ai-chat` 云函数需要 HTTP 请求能力。请使用 `axios`。

`cloud/functions/ai-chat/package.json`：

```json
{
  "name": "ai-chat",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "latest",
    "axios": "^1.6.0"
  }
}
```

### 12.6 代码要求

请实现完整：

```text
cloud/functions/ai-chat/index.js
```

参考代码如下，可以在此基础上结合实际项目优化：

```js
const cloud = require('wx-server-sdk')
const axios = require('axios')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = cloud.database()

function getCurrentMonth() {
  const now = new Date()
  const year = now.getFullYear()
  const month = String(now.getMonth() + 1).padStart(2, '0')
  return `${year}-${month}`
}

function toMoney(value) {
  return Number(Number(value || 0).toFixed(2))
}

async function getAllRecords(openid, month) {
  const pageSize = 100
  let skip = 0
  let all = []

  while (true) {
    const res = await db.collection('records')
      .where({
        _openid: openid,
        month
      })
      .skip(skip)
      .limit(pageSize)
      .get()

    const data = res.data || []
    all = all.concat(data)

    if (data.length < pageSize) {
      break
    }

    skip += pageSize
  }

  return all
}

function buildBillSummary(records, month) {
  const incomeRecords = records.filter(item => item.type === 'income')
  const expenseRecords = records.filter(item => item.type === 'expense')

  const totalIncome = incomeRecords.reduce((sum, item) => {
    return sum + Number(item.amount || 0)
  }, 0)

  const totalExpense = expenseRecords.reduce((sum, item) => {
    return sum + Number(item.amount || 0)
  }, 0)

  const categoryMap = {}
  const dailyMap = {}

  expenseRecords.forEach(item => {
    const amount = Number(item.amount || 0)
    const categoryName = item.categoryName || '其他'

    if (!categoryMap[categoryName]) {
      categoryMap[categoryName] = {
        name: categoryName,
        amount: 0,
        count: 0
      }
    }

    categoryMap[categoryName].amount += amount
    categoryMap[categoryName].count += 1

    const day = item.date ? item.date.slice(5) : ''

    if (day) {
      if (!dailyMap[day]) {
        dailyMap[day] = {
          date: day,
          expense: 0
        }
      }

      dailyMap[day].expense += amount
    }
  })

  const categoryStats = Object.values(categoryMap)
    .map(item => {
      return {
        name: item.name,
        amount: toMoney(item.amount),
        count: item.count,
        percentage: totalExpense > 0
          ? Number((item.amount / totalExpense * 100).toFixed(1))
          : 0
      }
    })
    .sort((a, b) => b.amount - a.amount)

  const dailyStats = Object.values(dailyMap)
    .map(item => {
      return {
        date: item.date,
        expense: toMoney(item.expense)
      }
    })
    .sort((a, b) => a.date.localeCompare(b.date))

  const recent7Days = dailyStats.slice(-7)

  const topExpenses = expenseRecords
    .slice()
    .sort((a, b) => Number(b.amount || 0) - Number(a.amount || 0))
    .slice(0, 5)
    .map(item => {
      return {
        date: item.date,
        categoryName: item.categoryName || '其他',
        amount: toMoney(item.amount),
        remark: item.remark || ''
      }
    })

  return {
    month,
    totalIncome: toMoney(totalIncome),
    totalExpense: toMoney(totalExpense),
    balance: toMoney(totalIncome - totalExpense),
    recordCount: records.length,
    categoryStats,
    recent7Days,
    topExpenses
  }
}

function buildMessages(userMessage, summary) {
  const categoryText = summary.categoryStats.length
    ? summary.categoryStats.map(item => {
      return `- ${item.name}：${item.amount} 元，占比 ${item.percentage}%，${item.count} 笔`
    }).join('\n')
    : '暂无分类支出数据'

  const recent7DaysText = summary.recent7Days.length
    ? summary.recent7Days.map(item => {
      return `- ${item.date}：${item.expense} 元`
    }).join('\n')
    : '暂无最近7天支出数据'

  const topExpensesText = summary.topExpenses.length
    ? summary.topExpenses.map(item => {
      return `- ${item.date}，${item.categoryName}，${item.amount} 元，备注：${item.remark || '无'}`
    }).join('\n')
    : '暂无大额支出数据'

  return [
    {
      role: 'system',
      content: [
        '你是一个个人记账小程序里的 AI 账单分析助手。',
        '',
        '规则：',
        '1. 只能基于系统提供的账单摘要回答。',
        '2. 不得编造不存在的金额、日期、分类、交易和备注。',
        '3. 如果数据不足，直接说明数据不足。',
        '4. 不要说你直接访问了数据库。',
        '5. 不要输出数据库字段名。',
        '6. 回答要具体，不要只说“理性消费”“合理规划”。',
        '7. 如果用户问省钱建议，要结合具体分类和金额给建议。',
        '8. 回答使用中文，语气自然、简洁。'
      ].join('\n')
    },
    {
      role: 'user',
      content: [
        `用户问题：${userMessage}`,
        '',
        '本月账单摘要：',
        `月份：${summary.month}`,
        `总收入：${summary.totalIncome} 元`,
        `总支出：${summary.totalExpense} 元`,
        `结余：${summary.balance} 元`,
        `账单笔数：${summary.recordCount}`,
        '',
        '分类支出：',
        categoryText,
        '',
        '最近7天支出：',
        recent7DaysText,
        '',
        '本月大额支出 Top5：',
        topExpensesText,
        '',
        '请基于以上账单摘要回答用户问题。'
      ].join('\n')
    }
  ]
}

async function callDeepSeek(messages) {
  const baseUrl = process.env.DEEPSEEK_BASE_URL || 'https://api.deepseek.com'
  const apiKey = process.env.DEEPSEEK_API_KEY
  const model = process.env.DEEPSEEK_MODEL || 'deepseek-chat'

  if (!apiKey) {
    throw new Error('未配置 DEEPSEEK_API_KEY')
  }

  const url = `${baseUrl.replace(/\/$/, '')}/chat/completions`

  const res = await axios.post(
    url,
    {
      model,
      messages,
      stream: false,
      temperature: 0.3,
      max_tokens: 1200
    },
    {
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${apiKey}`
      },
      timeout: 30000
    }
  )

  if (
    !res.data ||
    !res.data.choices ||
    !res.data.choices[0] ||
    !res.data.choices[0].message ||
    !res.data.choices[0].message.content
  ) {
    throw new Error('DeepSeek 返回格式异常')
  }

  return res.data.choices[0].message.content
}

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  const openid = wxContext.OPENID

  const message = (event.message || '').trim()

  if (!message) {
    return {
      success: false,
      reply: '问题不能为空',
      message: '问题不能为空'
    }
  }

  try {
    const month = getCurrentMonth()
    const records = await getAllRecords(openid, month)
    const summary = buildBillSummary(records, month)
    const messages = buildMessages(message, summary)
    const reply = await callDeepSeek(messages)

    return {
      success: true,
      reply,
      summary
    }
  } catch (error) {
    console.error('ai-chat error:', error)

    return {
      success: false,
      reply: 'AI 分析失败，请检查 DeepSeek API 配置或稍后再试。',
      message: error.message
    }
  }
}
```

---

## 13. 新增 AI 对话页面

请新增：

```text
pages/ai-chat/ai-chat.js
pages/ai-chat/ai-chat.wxml
pages/ai-chat/ai-chat.wxss
pages/ai-chat/ai-chat.json
```

### 13.1 修改 app.json

在 `pages` 数组中增加：

```json
"pages/ai-chat/ai-chat"
```

注意不要破坏原有页面顺序和 tabBar。

### 13.2 ai-chat.json

```json
{
  "navigationBarTitleText": "AI账单助手",
  "navigationBarBackgroundColor": "#0052d9",
  "navigationBarTextStyle": "white"
}
```

### 13.3 ai-chat.js

请实现：

```js
Page({
  data: {
    messages: [],
    inputValue: '',
    loading: false,
    quickQuestions: [
      '分析本月消费',
      '餐饮花得多吗',
      '本月最大几笔支出是什么',
      '最近7天花销怎么样',
      '我这个月还有什么要注意',
      '给我三个省钱建议'
    ]
  },

  onInput(e) {
    this.setData({
      inputValue: e.detail.value
    })
  },

  onQuickQuestion(e) {
    const question = e.currentTarget.dataset.question

    this.setData({
      inputValue: question
    })

    this.sendMessage()
  },

  sendMessage() {
    const content = this.data.inputValue.trim()

    if (!content || this.data.loading) {
      return
    }

    const userMessage = {
      role: 'user',
      content
    }

    this.setData({
      messages: this.data.messages.concat(userMessage),
      inputValue: '',
      loading: true
    })

    wx.cloud.callFunction({
      name: 'ai-chat',
      data: {
        message: content
      },
      success: (res) => {
        const result = res.result || {}

        const assistantMessage = {
          role: 'assistant',
          content: result.reply || result.message || '没有拿到有效回复'
        }

        this.setData({
          messages: this.data.messages.concat(assistantMessage),
          loading: false
        })
      },
      fail: (err) => {
        console.error('AI 请求失败:', err)

        const assistantMessage = {
          role: 'assistant',
          content: 'AI 请求失败，请稍后再试。'
        }

        this.setData({
          messages: this.data.messages.concat(assistantMessage),
          loading: false
        })
      }
    })
  },

  clearMessages() {
    wx.showModal({
      title: '清空对话',
      content: '确定要清空当前对话吗？',
      success: (res) => {
        if (res.confirm) {
          this.setData({
            messages: []
          })
        }
      }
    })
  }
})
```

### 13.4 ai-chat.wxml

请实现一个能用的聊天界面：

```xml
<view class="page">
  <scroll-view class="message-list" scroll-y="true">
    <view wx:if="{{messages.length === 0}}" class="empty">
      <view class="empty-title">AI账单助手</view>
      <view class="empty-desc">可以问我本月消费、分类支出、大额支出和省钱建议。</view>
    </view>

    <view
      wx:for="{{messages}}"
      wx:key="index"
      class="message {{item.role === 'user' ? 'message-user' : 'message-assistant'}}"
    >
      <view class="bubble">
        {{item.content}}
      </view>
    </view>

    <view wx:if="{{loading}}" class="message message-assistant">
      <view class="bubble">分析中...</view>
    </view>
  </scroll-view>

  <view class="quick-list">
    <view
      wx:for="{{quickQuestions}}"
      wx:key="index"
      class="quick-item"
      data-question="{{item}}"
      bindtap="onQuickQuestion"
    >
      {{item}}
    </view>
  </view>

  <view class="input-bar">
    <input
      class="input"
      value="{{inputValue}}"
      placeholder="问问你的账单情况"
      bindinput="onInput"
      confirm-type="send"
      bindconfirm="sendMessage"
    />
    <button class="send-btn" bindtap="sendMessage" loading="{{loading}}">
      发送
    </button>
  </view>
</view>
```

如果当前项目已有自己的 UI 风格，请适配原项目风格，但保证页面可用。

### 13.5 ai-chat.wxss

请实现基础样式：

```css
.page {
  min-height: 100vh;
  background: #f5f7fa;
  display: flex;
  flex-direction: column;
}

.message-list {
  flex: 1;
  padding: 24rpx;
  box-sizing: border-box;
}

.empty {
  margin-top: 120rpx;
  text-align: center;
  color: #666;
}

.empty-title {
  font-size: 40rpx;
  font-weight: 600;
  color: #222;
  margin-bottom: 16rpx;
}

.empty-desc {
  font-size: 28rpx;
  color: #888;
  padding: 0 48rpx;
  line-height: 1.6;
}

.message {
  display: flex;
  margin-bottom: 24rpx;
}

.message-user {
  justify-content: flex-end;
}

.message-assistant {
  justify-content: flex-start;
}

.bubble {
  max-width: 75%;
  padding: 20rpx 24rpx;
  border-radius: 18rpx;
  font-size: 28rpx;
  line-height: 1.6;
  white-space: pre-wrap;
  word-break: break-all;
}

.message-user .bubble {
  background: #0052d9;
  color: #fff;
  border-bottom-right-radius: 6rpx;
}

.message-assistant .bubble {
  background: #fff;
  color: #222;
  border-bottom-left-radius: 6rpx;
}

.quick-list {
  display: flex;
  overflow-x: auto;
  padding: 16rpx 24rpx;
  background: #f5f7fa;
  white-space: nowrap;
}

.quick-item {
  flex-shrink: 0;
  padding: 12rpx 20rpx;
  margin-right: 16rpx;
  background: #fff;
  border-radius: 999rpx;
  font-size: 24rpx;
  color: #0052d9;
}

.input-bar {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  background: #fff;
  border-top: 1rpx solid #eee;
}

.input {
  flex: 1;
  height: 72rpx;
  padding: 0 24rpx;
  background: #f5f7fa;
  border-radius: 36rpx;
  font-size: 28rpx;
}

.send-btn {
  margin-left: 16rpx;
  width: 140rpx;
  height: 72rpx;
  line-height: 72rpx;
  border-radius: 36rpx;
  background: #0052d9;
  color: #fff;
  font-size: 28rpx;
}
```

---

## 14. 增加 AI 入口

不要把 AI 加到底部 tabBar。

请优先在“我的”页增加入口。

修改：

```text
pages/profile/profile.js
```

在 `menuList` 中增加：

```js
{
  id: 'ai',
  title: 'AI账单助手',
  icon: '🤖',
  url: '/pages/ai-chat/ai-chat'
}
```

如果首页有快捷操作区，也可以增加一个“AI分析”按钮。

但必须保证：

```text
1. app.json 已注册 ai-chat 页面。
2. 点击入口可以 navigateTo 到 /pages/ai-chat/ai-chat。
3. 不要破坏原有预算管理、分类管理、设置、关于入口。
```

---

## 15. app.js 云环境配置

请检查：

```text
app.js
```

如果看到：

```js
env: 'your-env-id'
```

不要编造真实 env。

请保留占位符，但增加清晰注释：

```js
wx.cloud.init({
  env: 'your-env-id', // TODO: 替换为你自己的微信云开发环境 ID
  traceUser: true
})
```

如果项目中已有真实 env，不要覆盖。

---

## 16. 文档补充

请新增或更新一个文档文件：

```text
AI_DEVELOPMENT.md
```

内容包括：

```text
1. 功能说明。
2. 新增云数据库集合 records。
3. 新增云函数 record-add、statistics-get、ai-chat。
4. DeepSeek API 环境变量配置。
5. 微信开发者工具中上传并部署云函数的步骤。
6. records 集合权限建议。
7. 测试步骤。
8. 常见错误。
```

文档中不要写真实 DeepSeek API Key。

### 16.1 文档需要包含的环境变量说明

```text
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=你的 DeepSeek API Key
DEEPSEEK_MODEL=deepseek-chat
```

说明：

```text
DEEPSEEK_MODEL 可以根据 DeepSeek 官方当前可用模型自行调整。
```

### 16.2 文档需要包含的部署步骤

```text
1. 在微信开发者工具中打开项目。
2. 确认 app.js 中 env 已替换为自己的云开发环境 ID。
3. 在云开发控制台创建 records 集合。
4. 设置 records 集合权限，个人项目可先使用仅创建者可读写。
5. 右键 cloud/functions/record-add，上传并部署：云端安装依赖。
6. 右键 cloud/functions/statistics-get，上传并部署：云端安装依赖。
7. 右键 cloud/functions/ai-chat，上传并部署：云端安装依赖。
8. 在 ai-chat 云函数环境变量里配置 DEEPSEEK_API_KEY。
9. 编译小程序。
10. 新增一条支出记录。
11. 打开统计页，确认统计数据变化。
12. 打开 AI 账单助手，输入“分析本月消费”。
```

---

## 17. 测试要求

完成代码后，请尽量进行静态检查和逻辑检查。

至少检查：

```text
1. app.json 是否是合法 JSON。
2. 新增页面路径是否和文件路径一致。
3. wx.cloud.callFunction 的 name 是否和云函数目录名一致。
4. record-add 云函数是否写入 month 字段。
5. statistics-get 是否按 _openid 和 month 查询。
6. ai-chat 是否没有把 DeepSeek API Key 写死。
7. ai-chat 是否能在没有账单记录时返回合理回答。
8. 前端 loading 状态是否能正确关闭。
9. 保存记录失败时是否有错误提示。
10. AI 请求失败时是否有错误提示。
```

特殊情况测试：

```text
1. 当本月没有任何 records 时，AI 应该说明暂无账单数据，而不是编造消费。
2. 当 DeepSeek API Key 没配置时，ai-chat 应返回“未配置 DEEPSEEK_API_KEY”相关错误。
3. 当用户输入空问题时，不应调用云函数。
4. 当统计页没有数据时，不应白屏。
5. 当 records 里只有收入没有支出时，分类支出应为空，总支出为 0。
6. 当 records 里只有支出没有收入时，结余应为负数。
```

---

## 18. 长时间工作优先级

如果时间充足，请按以下优先级推进：

### P0：必须完成

```text
1. record-add 云函数。
2. 记账页真实入库。
3. statistics-get 云函数。
4. 统计页真实读取数据。
5. ai-chat 云函数。
6. AI 对话页面。
7. 我的页 AI 入口。
8. AI_DEVELOPMENT.md 文档。
```

### P1：强烈建议完成

```text
1. 首页真实读取月度概览和最近记录。
2. 统计页空数据状态处理。
3. AI 页面空数据状态处理。
4. 云函数错误信息更清晰。
5. README 中增加 AI 功能入口说明。
```

### P2：有余力再做

```text
1. AI 页面支持清空当前对话。
2. AI 回答区域增加复制按钮。
3. statistics-get 支持上月对比。
4. ai-chat 摘要中加入上月支出对比。
5. 预算数据接入 AI 摘要。
```

第一晚如果无法全部完成，请优先保证 P0。

---

## 19. 不要做的事

请明确不要做以下内容：

```text
1. 不要引入独立后端。
2. 不要引入数据库以外的复杂存储。
3. 不要把 DeepSeek API Key 写到前端。
4. 不要做 AI 设置页。
5. 不要做复杂意图识别。
6. 不要做多用户配置中心。
7. 不要做 Agent 编排。
8. 不要做 AI 自动生成数据库查询。
9. 不要删除现有页面。
10. 不要重写整个项目。
11. 不要为了“架构优雅”大幅改变现有 UI 和页面结构。
```

---

## 20. 最终交付要求

完成后，请输出一份最终说明，至少包括：

```text
1. 实际修改了哪些文件。
2. 新增了哪些文件。
3. 新增了哪些云函数。
4. 需要用户在微信开发者工具中手动做哪些事。
5. 需要用户在腾讯云 / 微信云开发控制台中配置哪些环境变量。
6. 如何测试完整链路。
7. 当前仍未完成的事项。
8. 可能的风险点。
```

最终说明格式建议：

```md
# 开发完成说明

## 已完成
...

## 修改文件
...

## 新增文件
...

## 云函数
...

## 需要手动配置
...

## 测试步骤
...

## 注意事项
...
```

---

## 21. 验收标准

只有满足以下条件，才算这次任务基本完成：

```text
1. 小程序可以新增一条记账记录，并通过 record-add 写入 records 集合。
2. records 中每条记录都有 _openid、type、amount、date、month、categoryName。
3. 统计页不再使用写死模拟数据，而是调用 statistics-get。
4. 首页如果改造了，也不再使用写死模拟数据。
5. 小程序中可以进入 AI账单助手页面。
6. AI账单助手页面可以输入问题并调用 ai-chat 云函数。
7. ai-chat 云函数不会暴露 DeepSeek API Key。
8. ai-chat 云函数会读取本月 records，整理摘要后调用 DeepSeek。
9. DeepSeek 返回内容可以显示在小程序页面。
10. 没有账单数据、API Key 未配置、云函数调用失败时都有基本错误提示。
11. 有 AI_DEVELOPMENT.md 部署说明。
```

---

## 22. 一句话执行方向

请记住本次任务不是“做一个复杂 AI 产品”，而是：

```text
把当前记账小程序从模拟数据推进到真实云数据库数据，并在此基础上增加一个 DeepSeek 账单分析入口。
```

优先打通真实链路：

```text
记账 → 入库 → 统计 → 摘要 → DeepSeek → 展示
```
